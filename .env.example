# Local laptop demo keeps using Express + SQLite through the Vite proxy.
VITE_DATA_BACKEND=local

# Portfolio deployment on Vercel should use:
# VITE_DATA_BACKEND=supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
